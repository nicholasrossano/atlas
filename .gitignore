# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
firebase-debug.log*
firebase-debug.*.log*
public/config.js

# Firebase cache
.firebase/

# Firebase config

# Uncomment this if you'd like others to create their own Firebase project.
# For a team working on the same Firebase project(s), it is recommended to leave
# it commented so all members can deploy to the same project(s) in .firebaserc.
# .firebaserc

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env

# dataconnect generated files
.dataconnect

# Local runtime config
public/config.js

# ─────────── Firebase Functions env + secrets ───────────
# Firebase Functions now loads env files from functions/ (and can auto-create .env.<projectId>)
.env.*
!.env.example
.secret.local
.secret.*.local

# If you want to be extra explicit / scoped:
functions/.env
functions/.env.*
!functions/.env.example
functions/.secret.local
functions/.secret.*.local

# ─────────── Emulator persisted data (if you use --import/--export-on-exit) ───────────
emulator-data/
firebase-export/
saved-data/

# ─────────── TypeScript build artifacts (only if you chose TS) ───────────
functions/lib/
functions/dist/
*.tsbuildinfo

# ─────────── Keys you never want in git ───────────
serviceAccount*.json
firebase-adminsdk*.json
*.pem
*.p12

# ─────────── OS junk ───────────
.DS_Store